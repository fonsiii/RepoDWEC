/**
 * Implemente la siguiente formula dandole valores a las variables
 * x=y^5+y/2
 */
const x = 0;
const y = 3;
x = y^5 + y/2;
console.log(x);

/**
 * total = total + total * 15%
 */
const total = 5;
const res = 0;
res = total + total * 0.15;
console.log(Math.floor(res));

/**
 * tasa = sqrt(x^2 + total * 7%)
 */
const tasa = 0;
tasa = Math.sqrt(x^2 + total * 0.07);
console.log(tasa);